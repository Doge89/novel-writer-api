import { IsValidPasswordPipe } from './is-valid-password.pipe';

describe('IsValidPasswordPipe', () => {
  it('should be defined', () => {
    expect(new IsValidPasswordPipe()).toBeDefined();
  });
});
