include:
  - database.yaml
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: on-failure
    depends_on:
      - postgresql-database
      - redis-database
    ports:
      - "3000:3000"